<Window x:Class="FilmDB.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="S

        ettings" Height="450" Width="800"
        ResizeMode="NoResize">
    <Grid>
        <TabControl>
            <TabItem Header="Movie Genres">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="GenreInputTextBox" Width="200" />
                        <Button Content="Add" Click="AddGenreButton_Click" Margin="10,0,0,0" />
                        <Button Content="Update" Click="UpdateGenreButton_Click" Margin="10,0,0,0" />
                        <Button Content="Remove" Click="RemoveGenreButton_Click" Margin="10,0,0,0" />
                    </StackPanel>
                    <ListBox x:Name="GenreListBox" Height="200" />
                </StackPanel>
            </TabItem>
            <TabItem Header="Application Settings">
                <!-- Content for application settings goes here -->
            </TabItem>
        </TabControl>
    </Grid>
</Window>